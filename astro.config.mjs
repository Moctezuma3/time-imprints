// @ts-check
import { defineConfig } from "astro/config";

// Échafaudage de base. Config volontairement minimale ;
// chaque site pourra l'étendre (i18n, intégrations, adapter d'hébergement…).
export default defineConfig({
  // site: "https://exemple.ch", // à renseigner par site (canonical, sitemap)
});
